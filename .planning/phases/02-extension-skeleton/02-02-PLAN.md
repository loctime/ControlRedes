---
phase: 02-extension-skeleton
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - extension/popup.html
  - extension/popup.js
  - extension/popup.css
  - dist/**
autonomous: true
requirements:
  - INFRA-03

must_haves:
  truths:
    - "El popup comunica claramente que la carpeta fuente es administrada por el server local"
    - "La extension no intenta pedir acceso directo al filesystem"
    - "El usuario entiende desde el popup que nuevas-publicaciones/ vive del lado del server"
  artifacts:
    - path: "extension/popup.html"
      provides: "UI minima de setup"
---

<objective>
Agregar el popup minimo para comunicar el estado del server y dejar explicito que el filesystem sigue siendo propiedad del proceso Node local, evitando una integracion de File System Access API que hoy no aporta valor a esta arquitectura.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: UI minima de popup</name>
  <action>
Crear un popup con estado de conexion al server, estado de carpeta server-managed, y acciones utiles para esta fase.
  </action>
</task>

<task type="auto">
  <name>Task 2: Claridad de ownership del filesystem</name>
  <action>
Eliminar el flujo de picker/reconnect y comunicar claramente que `server/` maneja `nuevas-publicaciones/` y `publicaciones-anteriores/`.
  </action>
</task>

<task type="auto">
  <name>Task 3: Sincronizacion popup -> storage</name>
  <action>
El popup debe leer del estado persistido y actualizar `chrome.storage.local` con un estado `server_owned` para mantener consistencia arquitectonica entre server y extension.
  </action>
</task>

</tasks>

<success_criteria>
- El popup deja claro que la carpeta es administrada por el server local
- La extension no falla intentando abrir un picker no soportado
- El estado `server_owned` se mantiene entre aperturas del popup
</success_criteria>

<output>
Crear `.planning/phases/02-extension-skeleton/02-02-SUMMARY.md` al finalizar.
</output>

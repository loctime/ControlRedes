---
phase: 02-extension-skeleton
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - extension/popup.html
  - extension/popup.js
  - extension/popup.css
  - extension/lib/folder-handle.js
  - dist/**
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02

must_haves:
  truths:
    - "El popup permite elegir la carpeta nuevas-publicaciones/"
    - "El FileSystemDirectoryHandle se persiste y puede revalidarse"
    - "Si el permiso se pierde, el popup muestra estado de reconexion"
  artifacts:
    - path: "extension/popup.html"
      provides: "UI minima de setup"
    - path: "extension/lib/folder-handle.js"
      provides: "Persistencia y revalidacion del handle"
---

<objective>
Agregar el popup minimo para setup de carpeta y reconexion de permisos, dejando resuelto el cuello de botella del File System Access API antes de la fase de grabacion.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: UI minima de popup</name>
  <action>
Crear un popup con estado de conexion al server, carpeta seleccionada, permiso actual, y acciones `Seleccionar carpeta` / `Reconectar carpeta`.
  </action>
</task>

<task type="auto">
  <name>Task 2: Persistencia del FileSystemDirectoryHandle</name>
  <action>
Implementar almacenamiento del handle en IndexedDB y helpers para `queryPermission()` / `requestPermission()`.
  </action>
</task>

<task type="auto">
  <name>Task 3: Sincronizacion popup -> storage</name>
  <action>
El popup debe leer del estado persistido y actualizar `chrome.storage.local` con el estado de permiso/carpeta para que el service worker lo vea sin depender del DOM.
  </action>
</task>

</tasks>

<success_criteria>
- El usuario puede elegir `nuevas-publicaciones/` desde el popup
- La carpeta elegida persiste entre aperturas del popup
- Si Chrome reinicia y el permiso ya no esta activo, el popup muestra claramente `Reconnect folder`
</success_criteria>

<output>
Crear `.planning/phases/02-extension-skeleton/02-02-SUMMARY.md` al finalizar.
</output>

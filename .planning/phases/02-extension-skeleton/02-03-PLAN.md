---
phase: 02-extension-skeleton
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - extension/popup.js
  - extension/lib/login-checks.js
  - extension/content/login-detectors.js
  - dist/**
autonomous: true
requirements:
  - SETUP-03

must_haves:
  truths:
    - "El popup puede ejecutar una verificacion basica de login para Instagram y LinkedIn"
    - "La verificacion no publica ni muta estado de las redes"
    - "Los resultados quedan persistidos para sobrevivir reinicios del SW"
  artifacts:
    - path: "extension/lib/login-checks.js"
      provides: "Chequeos de sesion por plataforma"
    - path: "extension/content/login-detectors.js"
      provides: "Detectores DOM minimos para presencia de sesion"
---

<objective>
Agregar un chequeo liviano de sesion para Instagram y LinkedIn que reduzca incertidumbre antes de la futura automatizacion de Phase 4, sin adelantarse a esa implementacion.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Detectores de login por plataforma</name>
  <action>
Implementar chequeos no destructivos que abran o reutilicen tabs de Instagram y LinkedIn, inspeccionen la presencia de elementos indicativos de sesion activa, y devuelvan `logged_in`, `logged_out` o `unknown`.
  </action>
</task>

<task type="auto">
  <name>Task 2: Integracion en popup</name>
  <action>
Agregar un boton `Verificar logins` y mostrar el resultado por plataforma con estado simple.
  </action>
</task>

<task type="auto">
  <name>Task 3: Persistencia de resultados</name>
  <action>
Guardar la ultima verificacion en `chrome.storage.local` con timestamp para que sobreviva a cierres y reinicios del service worker.
  </action>
</task>

</tasks>

<success_criteria>
- El popup muestra estado para Instagram y LinkedIn
- La verificacion corre sin publicar ni modificar formularios
- Los resultados siguen visibles despues de reiniciar el service worker
</success_criteria>

<output>
Crear `.planning/phases/02-extension-skeleton/02-03-SUMMARY.md` al finalizar.
</output>

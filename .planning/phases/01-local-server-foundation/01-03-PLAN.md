---
phase: 01-local-server-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - server/lifecycle.js
  - server/server.js
autonomous: true
requirements:
  - FILE-02
  - FILE-03
  - FILE-04
  - TELE-02

must_haves:
  truths:
    - "El servidor lee el caption desde [nombre].caption.txt si existe; usa el nombre de archivo (sin .html) como fallback"
    - "Llamar markComplete(filename, platforms, status, videoSpecs) mueve el HTML a publicaciones-anteriores/"
    - "Junto al HTML movido se crea [nombre].meta.json con los campos: date, platforms, status, videoSpecs"
    - "El bot envía 'Grabando post.html...' cuando comienza a procesar un archivo"
    - "El bot envía 'Completado ✓' cuando el pipeline finaliza todos los archivos"
    - "Después del ciclo completo, state.pipeline vuelve a 'idle'"
  artifacts:
    - path: "server/lifecycle.js"
      provides: "Funciones de ciclo de vida: readCaption, markComplete, runPipeline"
      exports: ["readCaption", "markComplete", "runPipeline"]
    - path: "server/server.js"
      provides: "Importa lifecycle.js e integra runPipeline en la lógica del bot"
      contains: "runPipeline"
  key_links:
    - from: "server/lifecycle.js readCaption"
      to: "nuevas-publicaciones/[nombre].caption.txt"
      via: "fs.existsSync + fs.readFileSync"
      pattern: "caption\\.txt"
    - from: "server/lifecycle.js markComplete"
      to: "publicaciones-anteriores/"
      via: "fs.renameSync"
      pattern: "fs\\.renameSync|fs\\.rename"
    - from: "server/lifecycle.js markComplete"
      to: "[nombre].meta.json"
      via: "fs.writeFileSync con JSON.stringify"
      pattern: "\\.meta\\.json"
---

<objective>
Implementar el ciclo de vida completo de archivos: lectura de captions, archivado del HTML a publicaciones-anteriores/, escritura del .meta.json, y envío de status updates por Telegram a lo largo del pipeline.

Purpose: Cierra el loop del pipeline. Sin esto, los archivos nunca se archivan, no hay registro de qué se publicó, y el bot no reporta progreso al usuario.
Output: server/lifecycle.js con readCaption, markComplete, runPipeline. server.js actualizado para invocar runPipeline cuando el bot recibe "publica lo nuevo".
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/User/Desktop/Proyectos/ControlRedes/.planning/ROADMAP.md
@C:/Users/User/Desktop/Proyectos/ControlRedes/.planning/REQUIREMENTS.md
@C:/Users/User/Desktop/Proyectos/ControlRedes/CLAUDE.md
@C:/Users/User/Desktop/Proyectos/ControlRedes/.planning/phases/01-local-server-foundation/01-CONTEXT.md
@C:/Users/User/Desktop/Proyectos/ControlRedes/.planning/phases/01-local-server-foundation/01-01-SUMMARY.md
@C:/Users/User/Desktop/Proyectos/ControlRedes/.planning/phases/01-local-server-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Contratos de planes anteriores que este plan consume -->

Del objeto `state` en server/server.js:
```javascript
const state = {
  pipeline: 'idle',      // 'idle' | 'active'
  pendingFiles: [],      // string[] — nombres de archivo (sin ruta)
  activeFile: null,      // string | null
  chatId: process.env.CHAT_ID || null,
};
```

De server/telegram.js:
```javascript
async function sendStatus(bot, chatId, text): Promise<void>
// Envía text al chatId mediante bot. Captura errores internamente.
```

Export de server.js:
```javascript
module.exports = { app, state, watcher, getBot: () => bot, sendStatus };
```

Schema .meta.json requerido (de CLAUDE.md):
```javascript
{
  date: string,           // ISO8601
  platforms: string[],    // e.g. ["instagram", "linkedin"]
  status: "success" | "partial" | "failed",
  videoSpecs: {
    width: number,
    height: number,
    fps: number,
    codec: string,
    duration: number
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear lifecycle.js con readCaption, markComplete y runPipeline</name>
  <files>server/lifecycle.js</files>
  <read_first>
    - .planning/phases/01-local-server-foundation/01-CONTEXT.md (D-03, D-08, D-09, D-10 para entender el modelo mental del pipeline)
    - CLAUDE.md (sección File Structure Convention — schema de .meta.json, carpetas)
    - server/telegram.js (firma exacta de sendStatus para llamarla correctamente)
  </read_first>
  <behavior>
    - readCaption('post.html') cuando existe 'post.caption.txt' → retorna contenido del archivo (string, trimmed)
    - readCaption('post.html') cuando NO existe 'post.caption.txt' → retorna 'post' (nombre sin extensión .html)
    - readCaption('mi-video.html') sin caption → retorna 'mi-video'
    - markComplete('post.html', ['instagram', 'linkedin'], 'success', {width:1080,height:1920,fps:30,codec:'h264',duration:15}) → mueve post.html a publicaciones-anteriores/post.html, crea publicaciones-anteriores/post.meta.json
    - El .meta.json creado contiene todos los campos: date (ISO8601), platforms, status, videoSpecs con los valores pasados
    - markComplete lanza Error si el archivo origen no existe
    - runPipeline(files, state, bot, chatId) con files=['a.html','b.html'] → envía "Grabando a.html..." luego "Grabando b.html..." luego "Completado ✓"
    - runPipeline al terminar pone state.pipeline = 'idle' y state.activeFile = null
  </behavior>
  <action>
Crear server/lifecycle.js con el siguiente contenido:

```javascript
'use strict';
const fs = require('fs');
const path = require('path');
const { sendStatus } = require('./telegram');

const WATCH_DIR = path.resolve(__dirname, '..', 'nuevas-publicaciones');
const ARCHIVE_DIR = path.resolve(__dirname, '..', 'publicaciones-anteriores');

/**
 * Lee el caption para un archivo HTML.
 * Busca [nombre].caption.txt en la misma carpeta. Fallback: nombre sin extensión.
 * @param {string} filename - Nombre del archivo HTML (sin ruta)
 * @returns {string} Texto del caption
 */
function readCaption(filename) {
  const basename = filename.replace(/\.html$/i, '');
  const captionPath = path.join(WATCH_DIR, `${basename}.caption.txt`);
  if (fs.existsSync(captionPath)) {
    return fs.readFileSync(captionPath, 'utf8').trim();
  }
  return basename;
}

/**
 * Archiva un HTML publicado: lo mueve a publicaciones-anteriores/ y escribe .meta.json.
 * @param {string} filename - Nombre del archivo HTML (sin ruta)
 * @param {string[]} platforms - Plataformas publicadas, e.g. ['instagram', 'linkedin']
 * @param {'success'|'partial'|'failed'} status - Estado de la publicación
 * @param {{width:number, height:number, fps:number, codec:string, duration:number}} videoSpecs
 */
function markComplete(filename, platforms, status, videoSpecs) {
  const srcPath = path.join(WATCH_DIR, filename);
  if (!fs.existsSync(srcPath)) {
    throw new Error(`[lifecycle] Archivo no encontrado: ${srcPath}`);
  }

  // Asegurar que el directorio de archivo exista
  if (!fs.existsSync(ARCHIVE_DIR)) {
    fs.mkdirSync(ARCHIVE_DIR, { recursive: true });
  }

  const destPath = path.join(ARCHIVE_DIR, filename);
  fs.renameSync(srcPath, destPath);

  const basename = filename.replace(/\.html$/i, '');
  const metaPath = path.join(ARCHIVE_DIR, `${basename}.meta.json`);

  const meta = {
    date: new Date().toISOString(),
    platforms,
    status,
    videoSpecs: {
      width: videoSpecs.width,
      height: videoSpecs.height,
      fps: videoSpecs.fps,
      codec: videoSpecs.codec,
      duration: videoSpecs.duration,
    },
  };

  fs.writeFileSync(metaPath, JSON.stringify(meta, null, 2), 'utf8');
}

/**
 * Ejecuta el pipeline para una lista de archivos.
 * En Phase 1 simula el procesamiento — las fases 2-4 reemplazarán el cuerpo del loop.
 * Envía status updates por Telegram a lo largo del proceso.
 *
 * @param {string[]} files - Lista de filenames a procesar (en orden)
 * @param {object} state - Estado compartido del pipeline
 * @param {import('node-telegram-bot-api')} bot - Instancia del bot Telegram
 * @param {string} chatId - Chat ID destino para los mensajes
 */
async function runPipeline(files, state, bot, chatId) {
  try {
    for (const filename of files) {
      state.activeFile = filename;

      // TELE-02: status update — grabando
      await sendStatus(bot, chatId, `Grabando ${filename}...`);

      // Phase 2-4 hook point: aquí irá la lógica real de grabación y publicación.
      // Por ahora marcamos el archivo como completado con specs de placeholder.
      // Los valores reales vendrán del POST /api/video-ready (Phase 2+).
      const placeholderSpecs = {
        width: 1080,
        height: 1920,
        fps: 30,
        codec: 'h264',
        duration: 0,  // Phase 3 enviará la duración real
      };

      markComplete(filename, ['instagram', 'linkedin'], 'success', placeholderSpecs);

      state.pendingFiles = state.pendingFiles.filter(f => f !== filename);
    }

    // TELE-02: status update — completado
    await sendStatus(bot, chatId, 'Completado ✓');
  } catch (err) {
    console.error('[pipeline] Error durante pipeline:', err.message);
    await sendStatus(bot, chatId, `Error en pipeline: ${err.message}`);
  } finally {
    // Siempre resetear estado al terminar (éxito o falla)
    state.pipeline = 'idle';
    state.activeFile = null;
  }
}

module.exports = { readCaption, markComplete, runPipeline };
```

NOTAS CRÍTICAS:
- `markComplete` usa `fs.renameSync` — falla si origen y destino están en diferentes volúmenes. En Windows, `nuevas-publicaciones/` y `publicaciones-anteriores/` son subdirectorios del mismo proyecto, mismo volumen → renameSync funciona.
- `runPipeline` llama `markComplete` con specs de placeholder en Phase 1. En Phase 3, el Offscreen Document enviará las specs reales vía POST /api/video-ready, y este loop será reemplazado por un await de ese evento.
- El bloque `finally` garantiza que `state.pipeline` vuelve a `'idle'` incluso si el pipeline falla a mitad.
  </action>
  <verify>
    <automated>cd "C:/Users/User/Desktop/Proyectos/ControlRedes/server" && node -e "const m = require('./lifecycle.js'); console.assert(typeof m.readCaption === 'function'); console.assert(typeof m.markComplete === 'function'); console.assert(typeof m.runPipeline === 'function'); const c = m.readCaption('mi-video.html'); console.assert(c === 'mi-video', 'fallback caption fail: ' + c); console.log('lifecycle module ok')"</automated>
  </verify>
  <acceptance_criteria>
    - server/lifecycle.js existe
    - Contiene: `module.exports = { readCaption, markComplete, runPipeline }`
    - Contiene: `.caption.txt` como patrón de búsqueda del archivo de caption
    - Contiene: `fs.renameSync` para mover el archivo al archivo
    - Contiene: `.meta.json` en la ruta del archivo de metadatos
    - Contiene: `JSON.stringify(meta` para escribir el .meta.json
    - Contiene: `new Date().toISOString()` para el campo date
    - Contiene: `state.pipeline = 'idle'` en el bloque finally
    - Contiene: `'Grabando '` (string de status update para Telegram)
    - Contiene: `'Completado ✓'` (string de status update final)
    - `node -e "const m=require('./lifecycle.js'); console.assert(m.readCaption('x.html')==='x')"` retorna exit code 0
    - `node -e "const m=require('./lifecycle.js'); console.assert(typeof m.runPipeline==='function')"` retorna exit code 0
  </acceptance_criteria>
  <done>lifecycle.js creado con readCaption (con fallback), markComplete (mueve + escribe .meta.json), runPipeline (loop con Telegram updates).</done>
</task>

<task type="auto">
  <name>Task 2: Integrar runPipeline en server.js — disparar desde el bot</name>
  <files>server/server.js</files>
  <read_first>
    - server/server.js (leer completo para encontrar dónde el bot activa state.pipeline = 'active' y envía la lista de archivos)
    - server/lifecycle.js (firma exacta de runPipeline para llamarla correctamente)
    - server/telegram.js (confirmar que sendStatus está exportado y cómo se usa getBot())
  </read_first>
  <action>
Modificar server/server.js para integrar runPipeline en el flujo del bot:

1. **Agregar import de lifecycle** (después de los imports existentes):
```javascript
const { runPipeline } = require('./lifecycle');
```

2. **En telegram.js**, la función initBot actualmente pone state.pipeline = 'active' y envía la lista, pero no llama runPipeline porque lifecycle no existe aún en ese módulo. Hay dos formas de resolver la integración:

**Opción A (recomendada):** Pasar un callback `onTrigger` a `initBot` que server.js provee:

En server.js, al llamar initBot:
```javascript
bot = initBot(state, async (files, chatId) => {
  // Este callback es invocado por telegram.js cuando llega el comando válido
  await runPipeline(files, state, bot, chatId);
});
```

Para soportar esto, modificar telegram.js para aceptar el segundo parámetro `onTrigger` y llamarlo en lugar de (o después de) poner state.pipeline = 'active'.

**Alternativa si la Opción A requiere demasiado refactor:** En server.js, después de que el bot envíe la lista de archivos, pueden usarse eventos de EventEmitter. Sin embargo, la opción más simple y explícita para este tamaño de codebase es:

Modificar initBot en telegram.js para aceptar un segundo parámetro `onTrigger: async (files, chatId) => void`:

```javascript
// En telegram.js — agregar onTrigger al handler
function initBot(state, onTrigger) {
  // ...
  // Donde antes: (después de enviar la lista de archivos)
  if (onTrigger) {
    // No await — runPipeline corre en background, el bot puede seguir respondiendo
    onTrigger(files, fromChatId).catch(err => {
      console.error('[Telegram] Error en pipeline callback:', err.message);
    });
  }
  // ...
}
```

El executor DEBE elegir la forma más limpia de integración entre los dos módulos. La restricción es:
- `runPipeline` se llama exactamente una vez cuando llega "publica lo nuevo" con archivos válidos
- `runPipeline` recibe `(files, state, bot, chatId)` con los valores correctos
- `runPipeline` NO debe ser await-ed en el handler de Telegram (bloquearía el polling)

3. **Actualizar los exports de server.js** para incluir runPipeline:
```javascript
module.exports = { app, state, watcher, getBot: () => bot, sendStatus, runPipeline };
```

IMPORTANTE: Respetar estas restricciones al modificar server.js:
- El mensaje de arranque `"Server running on localhost:${PORT} | Watching nuevas-publicaciones/"` NO debe cambiar
- Los tres endpoints GET existentes NO deben cambiar
- Los dos stubs POST existentes NO deben cambiar
- state.pipeline debe ponerse a 'idle' solo desde lifecycle.js (finally block) — no desde server.js ni telegram.js
  </action>
  <verify>
    <automated>cd "C:/Users/User/Desktop/Proyectos/ControlRedes/server" && node -e "const s = require('./server.js'); console.assert(typeof s.runPipeline === 'function' || typeof require('./lifecycle.js').runPipeline === 'function', 'runPipeline not found'); console.log('pipeline integration ok'); process.exit(0);" 2>&1 | head -3</automated>
  </verify>
  <acceptance_criteria>
    - server/server.js contiene: `require('./lifecycle')`
    - server/server.js contiene: `runPipeline` en alguna llamada o referencia
    - server/lifecycle.js contiene: `module.exports = { readCaption, markComplete, runPipeline }`
    - server/telegram.js — si se modificó para aceptar onTrigger, la firma es `function initBot(state, onTrigger)` o similar
    - La lógica de llamada a runPipeline NO usa await en el handler del mensaje de Telegram (no bloquea el polling)
    - `node -e "require('./server.js')"` retorna exit code 0 sin errores de sintaxis
    - `node -e "require('./lifecycle.js')"` retorna exit code 0 sin errores de sintaxis
    - Arrancar el servidor completo con `node server.js` (sin .env) muestra el mensaje de startup y no lanza error de módulo no encontrado
  </acceptance_criteria>
  <done>server.js integra runPipeline. El pipeline completo de Phase 1 está conectado: bot recibe comando → llama runPipeline → envía Telegram updates → archiva archivos → resetea state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram msg → filesystem | Un atacante con acceso al bot podría intentar explotar el pipeline para mover archivos arbitrarios |
| server.js → lifecycle.js markComplete | filename viene originalmente del Telegram handler pero ha pasado por fs.readdirSync — solo puede ser un nombre de archivo real en WATCH_DIR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | lifecycle.js — markComplete path construction | mitigate | Construir srcPath con `path.join(WATCH_DIR, filename)` donde filename viene de `fs.readdirSync(WATCH_DIR).filter(f => f.endsWith('.html'))` — solo archivos reales del directorio. No aceptar filenames arbitrarios de requests HTTP. |
| T-03-02 | Information Disclosure | .meta.json escrito en publicaciones-anteriores/ | accept | Los .meta.json solo contienen metadatos de publicación (fecha, plataformas, status, specs de video) — sin tokens, credenciales ni PII del usuario. |
| T-03-03 | Denial of Service | runPipeline consume el hilo si hay muchos archivos | accept | En uso normal < 10 archivos por pipeline. Node.js single thread no es problema para operaciones sync de FS en este volumen. |
| T-03-04 | Repudiation | No hay log de qué archivos se publicaron y cuándo | mitigate | El .meta.json con `date: ISO8601` provee auditoría por archivo. Adicionalmente, los mensajes de Telegram ("Grabando X...", "Completado") quedan en el historial del chat. |
| T-03-05 | Elevation of Privilege | Callback onTrigger pasado a initBot podría ser manipulado | accept | initBot y server.js son código local del servidor — no hay superficie de ataque externa para modificar el callback. |
</threat_model>

<verification>
```bash
# Test de readCaption (sin servidor corriendo):
cd server && node -e "
const { readCaption } = require('./lifecycle.js');
const fs = require('fs');
const path = require('path');

// Setup temporal
const watchDir = path.resolve(__dirname, '..', 'nuevas-publicaciones');
fs.mkdirSync(watchDir, { recursive: true });
fs.writeFileSync(path.join(watchDir, 'test.caption.txt'), 'Mi caption de prueba');

// Test con caption
const c1 = readCaption('test.html');
console.assert(c1 === 'Mi caption de prueba', 'caption read fail: ' + c1);
console.log('readCaption con .txt: ok');

// Test sin caption (fallback)
const c2 = readCaption('sin-caption.html');
console.assert(c2 === 'sin-caption', 'fallback fail: ' + c2);
console.log('readCaption fallback: ok');

// Cleanup
fs.unlinkSync(path.join(watchDir, 'test.caption.txt'));
"

# Test de markComplete:
node -e "
const { markComplete } = require('./lifecycle.js');
const fs = require('fs');
const path = require('path');

const watchDir = path.resolve(__dirname, '..', 'nuevas-publicaciones');
const archiveDir = path.resolve(__dirname, '..', 'publicaciones-anteriores');
fs.mkdirSync(watchDir, { recursive: true });

// Crear archivo de prueba
fs.writeFileSync(path.join(watchDir, 'prueba.html'), '<html>test</html>');

// Llamar markComplete
markComplete('prueba.html', ['instagram', 'linkedin'], 'success', {
  width: 1080, height: 1920, fps: 30, codec: 'h264', duration: 15
});

// Verificar que se movió
console.assert(!fs.existsSync(path.join(watchDir, 'prueba.html')), 'html no movido');
console.assert(fs.existsSync(path.join(archiveDir, 'prueba.html')), 'html no archivado');

// Verificar .meta.json
const meta = JSON.parse(fs.readFileSync(path.join(archiveDir, 'prueba.meta.json'), 'utf8'));
console.assert(meta.status === 'success', 'status fail');
console.assert(meta.platforms.includes('instagram'), 'platforms fail');
console.assert(meta.videoSpecs.width === 1080, 'videoSpecs fail');
console.assert(meta.date.includes('T'), 'date not ISO8601');
console.log('markComplete: ok');

// Cleanup
fs.unlinkSync(path.join(archiveDir, 'prueba.html'));
fs.unlinkSync(path.join(archiveDir, 'prueba.meta.json'));
"
```
</verification>

<success_criteria>
- readCaption('post.html') con post.caption.txt existente retorna el contenido del archivo (trimmed)
- readCaption('post.html') sin caption.txt retorna 'post' (nombre sin .html)
- markComplete('post.html', [...], 'success', {...}) mueve post.html a publicaciones-anteriores/post.html
- publicaciones-anteriores/post.meta.json contiene: date (ISO8601), platforms (array), status ('success'), videoSpecs ({width, height, fps, codec, duration})
- runPipeline procesa todos los archivos y al terminar: state.pipeline === 'idle', state.activeFile === null
- El bot envía 'Grabando post.html...' antes de procesar ese archivo
- El bot envía 'Completado ✓' al final del pipeline
- `node server.js` arranca sin errores con los tres módulos integrados
</success_criteria>

<output>
Después de completar este plan, crear `.planning/phases/01-local-server-foundation/01-03-SUMMARY.md` con:
- Funciones implementadas en lifecycle.js y sus firmas
- Cómo se integró runPipeline en server.js (via callback, EventEmitter, u otro patrón elegido)
- Schema exacto del .meta.json que se escribe
- Cómo se llama a sendStatus en los diferentes puntos del pipeline
- Cualquier desviación del plan y por qué
</output>

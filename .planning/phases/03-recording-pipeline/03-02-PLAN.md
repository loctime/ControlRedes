---
phase: 03-recording-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - extension/service-worker.js
  - extension/offscreen.js
  - extension/lib/recording-timing.js
  - extension/lib/state.js
autonomous: true
requirements:
  - REC-02
  - REC-03
  - REC-04

must_haves:
  truths:
    - "La grabacion se detiene por evento gsd:done cuando existe"
    - "Si no hay evento, la grabacion termina por timeout configurable"
    - "Se agrega buffer posterior al fin antes de cortar"
  artifacts:
    - path: "extension/lib/recording-timing.js"
      provides: "Reglas de timeout y post-buffer"
---

<objective>
Implementar control de duracion de grabacion: fin por `gsd:done`, timeout fallback y post-buffer, sin cuelgues ni sesiones huérfanas.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Listener de fin de animacion</name>
  <action>
Detectar `window.parent.postMessage({ type: "gsd:done" }, "*")` en el contexto de captura y propagar evento de stop al offscreen recorder.
  </action>
</task>

<task type="auto">
  <name>Task 2: Timeout y buffer</name>
  <action>
Implementar timeout por archivo (default 60s) y buffer posterior configurable (por ejemplo 1-2s) antes de cerrar MediaRecorder.
  </action>
</task>

<task type="auto">
  <name>Task 3: Manejo de errores y limpieza</name>
  <action>
Asegurar teardown consistente de tab, stream y session state en rutas de success/timeout/error para evitar fugas.
  </action>
</task>

</tasks>

<success_criteria>
- HTML que emite `gsd:done` corta en done + buffer
- HTML que no emite `gsd:done` corta en timeout
- No quedan tabs ni streams abiertos luego de cada archivo
</success_criteria>

<output>
Crear `.planning/phases/03-recording-pipeline/03-02-SUMMARY.md` al finalizar.
</output>
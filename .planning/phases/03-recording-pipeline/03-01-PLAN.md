---
phase: 03-recording-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/manifest.json
  - extension/service-worker.js
  - extension/lib/constants.js
  - extension/lib/state.js
  - extension/offscreen.html
  - extension/offscreen.js
autonomous: true
requirements:
  - REC-01
  - REC-05

must_haves:
  truths:
    - "La extension puede inicializar un Offscreen Document unico para tareas de grabacion"
    - "El pipeline abre cada HTML objetivo en tab dedicada de Chrome"
    - "La resolucion de grabacion queda fijada en 1080x1920 en la ruta principal"
  artifacts:
    - path: "extension/offscreen.html"
      provides: "Documento offscreen para trabajo largo"
    - path: "extension/offscreen.js"
      provides: "Handshake de mensajes con service worker"
---

<objective>
Establecer la base tecnica de captura para Fase 3: wiring de offscreen document, orquestacion inicial desde service worker y contrato de mensajes para iniciar/terminar sesiones de grabacion por archivo.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Offscreen scaffolding y permisos</name>
  <action>
Agregar permisos y recursos necesarios en manifest para `chrome.offscreen` y registrar `offscreen.html` + `offscreen.js`.
  </action>
</task>

<task type="auto">
  <name>Task 2: Contrato SW <-> Offscreen</name>
  <action>
Implementar mensajes tipados para crear documento, iniciar sesion de grabacion y reportar progreso/error al service worker.
  </action>
</task>

<task type="auto">
  <name>Task 3: Estado persistente de sesion</name>
  <action>
Agregar campos en `chrome.storage.local` para registrar `recordingSession` (file, startedAt, phase, tabId, status) antes y despues de cada transicion.
  </action>
</task>

</tasks>

<success_criteria>
- Offscreen document se crea una sola vez y responde a ping
- Service worker puede iniciar una sesion de recording para un archivo objetivo
- Estado de sesion queda persistido y recuperable tras restart del SW
</success_criteria>

<output>
Crear `.planning/phases/03-recording-pipeline/03-01-SUMMARY.md` al finalizar.
</output>